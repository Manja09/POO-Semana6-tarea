[project]
name = "poo-semana6"
version = "0.1.0"
description = "Proyecto POO con Docker y GitHub Actions"
authors = [{ name = "Edgar" }]
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["test"]